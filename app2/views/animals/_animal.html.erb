<div class="container">
    <div class="col-sm-offset-3 col-sm-6">
        <ul class="list-group">
                <div class="list-group-item">
                    <h4><%= link_to animal.name, animal.route %> <%= t'.owner_html', owner: animal.usuario.nome_completo %></h4>
                    
                    <%= render partial: 'review', locals: {animal: animal} %>
                    
                    <div>
                        <% if animal.picture_url != nil %>
                            <img src="<%= animal.picture_url %>" class="img-circle img-responsive" />
                        <% end %><br />
                    </div>
                    <div><p>Espécie: 
		              <% if animal.specie == "D" %>
			             Cachorro
		              <% else %>
			             Gato
	                  <% end %>
                    </p></div>
                    
                    <% if belongs_to_user(animal) %>
                    <ul class="container">
                        <li class="btn"><%= link_to t('.edit'), edit_animal_path(animal.route), class: 'btn btn-primary' %></li>
                        <li class="btn"><%= link_to t('.destroy'), animal_path(animal.route), class: 'btn btn-danger', method: :delete, data: {confirm: t('dialogs.destroy')}  %></li>
                    </ul>
                    <% end %>
                </div>
        </ul>
    </div>
</div>
